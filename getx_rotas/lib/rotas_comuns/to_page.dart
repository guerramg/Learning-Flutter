import 'package:flutter/material.dart';

class ToPage extends StatelessWidget {

  const ToPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('GET TO'),),
           body: Container(),
       );
  }
}